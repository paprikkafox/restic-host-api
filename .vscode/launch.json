{
    "configurations": [
        {
            "name": "Debug Project",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/cmd/main.go",
        }
    ]
}